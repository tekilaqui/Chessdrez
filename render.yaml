services:
  - type: web
    name: chessdrez-api
    runtime: node
    buildCommand: npm install -g pnpm@9 && pnpm install && pnpm --filter api run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
