# Production Environment Variables Template

# --- DATABASE ---
# Hosted Postgres (Recommended: Neon.tech)
DATABASE_URL="postgres://user:password@hostname:5432/dbname?sslmode=require"

# --- AUTHENTICATION ---
# Secret for signing JWT tokens (Generate a strong random string)
JWT_SECRET="generate-a-very-secret-key-here"

# --- REDIS (Optional, for BullMQ/Queue) ---
# Required if using background analysis in production (Recommended: Upstash)
REDIS_URL="redis://default:password@hostname:6379"

# --- CORS / FRONTEND ---
# The URL where your frontend will be hosted (Cloudflare Pages URL)
FRONTEND_URL="https://chess-platform.pages.dev"

# --- APP CONFIG ---
PORT=3000
NODE_ENV=production
